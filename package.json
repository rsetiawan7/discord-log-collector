{
  "name": "discord-bot-log-collector",
  "version": "1.0.0",
  "description": "A script that collects log from UDP and send it to Discord by bot",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "npm run clean",
    "build": "tsc",
    "clean": "rimraf dist/",
    "start": "node dist/index.js",
    "watch": "node --inspect -r ts-node/register src/index.ts"
  },
  "author": "Rachmad Setiawan",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^12.5.3",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^12.20.16",
    "copyfiles": "^2.4.1",
    "nodemon": "^2.0.15",
    "rimraf": "^3.0.2",
    "ts-node": "^10.4.0",
    "tslint": "^6.1.3",
    "typescript": "^4.3.5"
  }
}
